import { Tag } from 'antd';
import styled from 'styled-components';

export const StyledTag = styled(Tag)`
	position: absolute;
	top: 12px;
	right: 12px;
	margin: 0;
	width: 52px;
	height: 52px;
	font-size: 20px;
	font-weight: 600;
	line-height: 52px;
	text-align: center;
`;
