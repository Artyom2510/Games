import React, { FC } from 'react';
import { TGameFullCardProps } from './types';

const FullCard: FC<TGameFullCardProps> = ({ info }) => {
	return (
		<div>
			<p>index</p>
		</div>
	);
};

export default FullCard;
